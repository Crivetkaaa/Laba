<Window x:Class="Laba.Window_for_ghost"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Laba"
        mc:Ignorable="d"
        Title="Window_for_ghost" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="ClientsGrid" AutoGenerateColumns="False" HorizontalGridLinesBrush="DarkGray" RowBackground="LightGray" AlternatingRowBackground="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Фамилия/login" Binding="{Binding Path=Surname_ghost}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Имя Фамилия" Binding="{Binding Path=Name_ghost}" Width="130" IsReadOnly="True"/>
                <DataGridTextColumn Header="Должность" Binding="{Binding Path=Post_ghost}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Рабочий телефон" Binding="{Binding Path=Worked_Phone_ghost}" Width="100" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
